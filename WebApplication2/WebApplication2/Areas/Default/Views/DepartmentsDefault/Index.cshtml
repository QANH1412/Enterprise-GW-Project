@model IEnumerable<WebApplication2.Models.Department>

@{
    Layout = "~/Areas/Default/Views/Shared/_LayoutDefault.cshtml"; ;
    var coordinator =  (WebApplication2.Models.User)HttpContext.Current.Session["user"];

}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
  
    <table class="table">
        <tr>
            <th>
                Name
            </th>
        </tr>
    
   @* @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
                @Html.ActionLink("Details", "Details", new { id=item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.Id })
            </td>
        </tr>
    }*@

        <tr>
            <td>
                @coordinator.DepartmentId
            </td>
        </tr>
    
    </table>
</body>
</html>
